{%- comment -%}
  AUTO-GENERATED: Shopify Discount Sync System
  Generated on: 2025-10-11T14:52:48.814994

  This file is automatically synchronized with Shopify Admin discount codes.
  Do not edit manually - use sync_discounts.py to update.

  Provides centralized discount lookup for both template previews and cart badges.

  Usage:
  - For product previews: {% render 'hubpro-discount-simple', collection: 'kartell', segment: 'diy' %}
  - For cart discounts: {% render 'hubpro-discount-simple', title: 'HUBPRO-KARTELL-FREE' %}

  Returns: percentage number (e.g., 35)
{%- endcomment -%}

{%- liquid
  assign percentage = 0

  # Handle title-based lookups (for cart discount badges)
  if title != blank

    if title contains '40%' or title contains 'SAVE 40%'
      assign percentage = 40

    elsif title contains '25%' or title contains 'SAVE 25%'
      assign percentage = 25

    elsif title contains '30%' or title contains 'SAVE 30%'
      assign percentage = 30

    elsif title contains '20%' or title contains 'SAVE 20%'
      assign percentage = 20

    elsif title contains '15%' or title contains 'SAVE 15%'
      assign percentage = 15

    elsif title contains '5%' or title contains 'tradehub5'
      assign percentage = 5

    elsif title contains '10%' or title contains 'HubPRO' or title contains 'hubpro' or title contains 'HUBPRO' or title contains 'HubPRO'
      assign percentage = 10
    endif

  # Handle collection-based lookups (applies to ALL customers)
  elsif collection != blank

    if collection contains 'nuie' or collection contains 'hudson-reed' or collection contains 'kartell'
      assign percentage = 40

    elsif collection contains 'perform-panel' or collection contains 'everbuild-sika' or collection contains 'hubchoice'
      assign percentage = 30

    elsif collection contains 'kudos-shower-trays' or collection contains 'viva' or collection contains 'viva-essentials' or collection contains 'soudal' or collection contains 'artesan-shower-trays'
      assign percentage = 25

    elsif collection contains 'multipanel-1'
      assign percentage = 20

    elsif collection contains 'decorfloor' or collection contains 'perform-trims-consumables' or collection contains 'decorfloor-sample-box-natural-wood' or collection contains 'decorfloor-sample-box-natural-stone' or collection contains 'decorfloor-natural-wood' or collection contains 'decorfloor-natural-stone' or collection contains 'decorwall' or collection contains 'nature-panel' or collection contains 'cladworks-pvc-panels' or collection contains 'the-decorwall-elite-range' or collection contains 'the-decorwall-classic-range'
      assign percentage = 15
    endif
  endif

  echo percentage
-%}