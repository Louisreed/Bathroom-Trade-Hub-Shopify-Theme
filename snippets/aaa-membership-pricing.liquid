{%- comment -%}
  AAA Membership Pricing Helper

  Calculates membership discount percentage for the current customer based on shop metafields.
  IMPORTANT: Use {% assign %} in parent scope to capture the result, not {% render %}

  Usage:
  {% liquid
    assign membership_discount_percent = 0
    assign has_membership_pricing = false
  %}
  {% include 'aaa-membership-pricing' %}
{%- endcomment -%}

{%- liquid
  assign membership_discount_percent = 0
  assign has_membership_pricing = false

  # Include price rules to check for membership pricing
  render 'aaa-plan_rule'

  # Check if customer has membership pricing access
  unless aaa_price_hide_item == true
    # First try aaa_mem_price (original format)
    for aaa_rule in shop.metafields.aaa_mem_price
      assign key_wrd = aaa_rule.last | split: ','

      # Check if this customer's tags match the membership rule
      for customer_tag in customer.tags
        assign clean_customer_tag = customer_tag | downcase | strip
        assign clean_rule_tag = key_wrd.first | downcase | strip

        if clean_customer_tag == clean_rule_tag
          # Extract discount percentage (e.g., "50%" -> 50)
          if key_wrd.size > 2
            assign discount_string = key_wrd[2] | remove: '%' | strip
            assign membership_discount_percent = discount_string | plus: 0
            assign has_membership_pricing = true
            break
          endif
        endif
      endfor

      if has_membership_pricing
        break
      endif
    endfor

    # If no discount found, try AtTrillion membership metafields
    unless has_membership_pricing
      for membership_rule in shop.metafields.membership
        assign rule_parts = membership_rule.last | split: ','

        for customer_tag in customer.tags
          assign clean_customer_tag = customer_tag | downcase | strip
          assign clean_rule_tag = rule_parts.first | downcase | strip

          if clean_customer_tag == clean_rule_tag
            if rule_parts.size > 2
              assign discount_string = rule_parts[2] | remove: '%' | strip
              assign membership_discount_percent = discount_string | plus: 0
              assign has_membership_pricing = true
              break
            endif
          endif
        endfor

        if has_membership_pricing
          break
        endif
      endfor
    endunless

    # If still no discount, try AtTrillion atrillion metafields
    unless has_membership_pricing
      for atrillion_rule in shop.metafields.atrillion
        assign rule_parts = atrillion_rule.last | split: ','

        for customer_tag in customer.tags
          assign clean_customer_tag = customer_tag | downcase | strip
          assign clean_rule_tag = rule_parts.first | downcase | strip

          if clean_customer_tag == clean_rule_tag
            if rule_parts.size > 2
              assign discount_string = rule_parts[2] | remove: '%' | strip
              assign membership_discount_percent = discount_string | plus: 0
              assign has_membership_pricing = true
              break
            endif
          endif
        endfor

        if has_membership_pricing
          break
        endif
      endfor
    endunless

    # COLLECTION-BASED DISCOUNT LOGIC: Customer eligibility + highest discount
    unless has_membership_pricing
      for customer_tag in customer.tags
        assign clean_customer_tag = customer_tag | downcase | strip
        if clean_customer_tag == 'hubpro-free'
          # Customer is eligible - find highest discount from collections
          if product
            assign temp_discount = 0
            assign temp_collection = ''

            for collection in product.collections
              assign collection_handle = collection.handle | downcase | strip
              assign collection_title = collection.title | downcase | strip
              assign current_discount = 0

              # Determine discount percentage for this collection
              if collection_handle == 'maxi-panels-1' or collection_title contains 'maxi panels'
                assign current_discount = 45
              elsif collection_handle contains 'wall' or collection_title contains 'wall'
                assign current_discount = 30
              elsif collection_handle contains 'chevron' or collection_title contains 'chevron'
                assign current_discount = 25
              elsif collection_handle == 'hubessentials' or collection_title contains 'hubessentials'
                assign current_discount = 15
              elsif collection_handle contains 'trim' or collection_title contains 'trim'
                assign current_discount = 10
              endif

              # Keep the highest discount found
              if current_discount > temp_discount
                assign temp_discount = current_discount
                assign temp_collection = collection.title
              endif
            endfor

            # Apply the highest discount found
            if temp_discount > 0
              assign membership_discount_percent = temp_discount
              assign has_membership_pricing = true
              assign matched_collection = temp_collection
            endif
          endif
          break
        endif
      endfor
    endunless
  endunless
-%}

{%- comment -%} COMPREHENSIVE DEBUG for AAA membership pricing {%- endcomment -%}
<script>
  if (window.console && window.console.log) {
    console.log('🚀 === AAA MEMBERSHIP PRICING DEBUG START ===');
    console.log('👤 CUSTOMER STATUS:', {
      loggedIn: {% if customer %}true{% else %}false{% endif %},
      customerId: {% if customer.id %}{{ customer.id | json }}{% else %}null{% endif %},
      customerEmail: {% if customer.email %}{{ customer.email | json }}{% else %}null{% endif %},
      customerName: '{% if customer %}{{ customer.first_name }} {{ customer.last_name }}{% else %}Not logged in{% endif %}',
      customerTags: {% if customer.tags %}{{ customer.tags | json }}{% else %}[]{% endif %},
      totalTags: {% if customer.tags %}{{ customer.tags.size }}{% else %}0{% endif %}
    });

    console.log('🏪 SHOP METAFIELDS (aaa_mem_price):');
    {% if shop.metafields.aaa_mem_price.size > 0 %}
      {% for aaa_rule in shop.metafields.aaa_mem_price %}
        console.log('  Rule {{ forloop.index }}: "{{ aaa_rule.first }}" = "{{ aaa_rule.last }}"');
        console.log('    Split parts:', '{{ aaa_rule.last }}'.split(','));
      {% endfor %}
    {% else %}
      console.log('  ❌ NO AAA PRICING METAFIELDS FOUND!');
    {% endif %}

    console.log('🔍 DEBUGGING ALL SHOP METAFIELDS:');
            console.log('🔍 TESTING SPECIFIC METAFIELD PATHS:');
    {% if shop.metafields.aaa_mem %}
      console.log('shop.metafields.aaa_mem: Found data');
    {% else %}
      console.log('shop.metafields.aaa_mem: Empty or missing');
    {% endif %}

    {% if shop.metafields.custom %}
      console.log('shop.metafields.custom: Found data');
    {% else %}
      console.log('shop.metafields.custom: Empty or missing');
    {% endif %}

    console.log('🔍 AAA APP INVESTIGATION - The Real Problem:');
    console.log('❌ AAA app has NOT saved discount rules to Shopify metafields');
    console.log('❌ Your discount config exists in AAA dashboard but not in Shopify');
    console.log('❌ AAA may use its own database instead of Shopify metafields');

    console.log('✅ SOLUTION: Using working hardcoded logic as bridge solution');

    console.log('🎯 CHECKING ATRILLION/MEMBERSHIP NAMESPACES:');
    {% if shop.metafields.membership %}
      console.log('  ✅ shop.metafields.membership exists');
      {% for rule in shop.metafields.membership %}
        console.log('    Membership rule: "{{ rule.first }}" = "{{ rule.last }}"');
        console.log('    Split parts:', '{{ rule.last }}'.split(','));
      {% endfor %}
    {% else %}
      console.log('  ❌ shop.metafields.membership not found');
    {% endif %}
    {% if shop.metafields.atrillion %}
      console.log('  ✅ shop.metafields.atrillion exists');
      {% for rule in shop.metafields.atrillion %}
        console.log('    AtTrillion rule: "{{ rule.first }}" = "{{ rule.last }}"');
        console.log('    Split parts:', '{{ rule.last }}'.split(','));
      {% endfor %}
    {% else %}
      console.log('  ❌ shop.metafields.atrillion not found');
    {% endif %}

    console.log('🔍 DETAILED METAFIELD INSPECTION:');
    console.log('Raw shop.metafields object:', {{ shop.metafields | json }});

    console.log('🔍 DIRECT NAMESPACE ACCESS:');
    {% if shop.metafields['membership'] %}
      console.log('shop.metafields["membership"]:', {{ shop.metafields['membership'] | json }});
    {% endif %}
    {% if shop.metafields['atrillion'] %}
      console.log('shop.metafields["atrillion"]:', {{ shop.metafields['atrillion'] | json }});
    {% endif %}
    {% if shop.metafields['aaa_mem_price'] %}
      console.log('shop.metafields["aaa_mem_price"]:', {{ shop.metafields['aaa_mem_price'] | json }});
    {% endif %}

    console.log('🔍 TRYING DOT NOTATION:');
    console.log('shop.metafields.membership:', {{ shop.metafields.membership | json }});
    console.log('shop.metafields.atrillion:', {{ shop.metafields.atrillion | json }});
    console.log('shop.metafields.aaa_mem_price:', {{ shop.metafields.aaa_mem_price | json }});

    console.log('🛍️ CURRENT PRODUCT COLLECTION INFO:');
    {% if product %}
      console.log('Product title:', {{ product.title | json }});
      console.log('Product collections:', {{ product.collections | map: 'handle' | json }});
      console.log('Product collections (titles):', {{ product.collections | map: 'title' | json }});
      {% for collection in product.collections %}
        console.log('  Collection: "{{ collection.handle }}" ({{ collection.title }})');
      {% endfor %}
    {% else %}
      console.log('❌ No product context available');
    {% endif %}

    console.log('🔄 PROCESSING ATRILLION TAG MATCHING:');
    {% for customer_tag in customer.tags %}
      console.log('  🏷️ Testing customer tag: "{{ customer_tag }}"');
      {% for membership_rule in shop.metafields.membership %}
        {% assign rule_parts = membership_rule.last | split: ',' %}
        {% assign rule_tag = rule_parts.first | downcase | strip %}
        console.log('    vs membership rule tag: "{{ rule_tag }}"');
        {% if customer_tag == rule_tag %}
          console.log('      ✅ MATCH! Rule: {{ membership_rule.last }}');
        {% endif %}
      {% endfor %}
      {% for atrillion_rule in shop.metafields.atrillion %}
        {% assign rule_parts = atrillion_rule.last | split: ',' %}
        {% assign rule_tag = rule_parts.first | downcase | strip %}
        console.log('    vs atrillion rule tag: "{{ rule_tag }}"');
        {% if customer_tag == rule_tag %}
          console.log('      ✅ MATCH! Rule: {{ atrillion_rule.last }}');
        {% endif %}
      {% endfor %}
    {% endfor %}

    console.log('🔍 TAG MATCHING PROCESS:');
    {% if customer.tags.size > 0 %}
      {% for customer_tag in customer.tags %}
        console.log('  🏷️ Customer Tag {{ forloop.index }}: "{{ customer_tag }}"');
        {% for aaa_rule in shop.metafields.aaa_mem_price %}
          {% assign key_wrd = aaa_rule.last | split: ',' %}
          {% assign clean_customer_tag = customer_tag | downcase | strip %}
          {% assign clean_rule_tag = key_wrd.first | downcase | strip %}
          console.log('    Comparing "{{ clean_customer_tag }}" vs "{{ clean_rule_tag }}"');
          {% if clean_customer_tag == clean_rule_tag %}
            console.log('    ✅ MATCH FOUND! Rule: {{ aaa_rule.last }}');
            {% if key_wrd.size > 2 %}
              console.log('    💰 Discount: {{ key_wrd[2] }}');
            {% else %}
              console.log('    ❌ No discount percentage found in rule');
            {% endif %}
          {% endif %}
        {% endfor %}
      {% endfor %}
    {% else %}
      console.log('  ❌ Customer has NO TAGS!');
    {% endif %}

        console.log('🎯 COLLECTION-BASED DISCOUNT MATCHING (finding HIGHEST discount):');
    {% if product %}
      {% assign temp_discount = 0 %}
      {% assign temp_collection = '' %}

      {% for collection in product.collections %}
        {% assign collection_handle = collection.handle | downcase | strip %}
        {% assign collection_title = collection.title | downcase | strip %}
        {% assign current_discount = 0 %}

        {% if collection_handle == 'maxi-panels-1' or collection_title contains 'maxi panels' %}
          {% assign current_discount = 45 %}
        {% elsif collection_handle contains 'wall' or collection_title contains 'wall' %}
          {% assign current_discount = 30 %}
        {% elsif collection_handle contains 'chevron' or collection_title contains 'chevron' %}
          {% assign current_discount = 25 %}
        {% elsif collection_handle == 'hubessentials' or collection_title contains 'hubessentials' %}
          {% assign current_discount = 15 %}
        {% elsif collection_handle contains 'trim' or collection_title contains 'trim' %}
          {% assign current_discount = 10 %}
        {% endif %}

        console.log('  Testing collection: "{{ collection.handle }}" ({{ collection.title }}) → {{ current_discount }}% discount');

        {% if current_discount > temp_discount %}
          {% assign temp_discount = current_discount %}
          {% assign temp_collection = collection.title %}
          console.log('    🎯 NEW HIGHEST: {{ current_discount }}% from {{ collection.title }}');
        {% endif %}
      {% endfor %}

      console.log('🏆 WINNING COLLECTION: "{{ temp_collection }}" with {{ temp_discount }}% discount');
    {% endif %}

    console.log('📊 FINAL CALCULATION RESULT:', {
      priceHideItem: {% if aaa_price_hide_item %}{{ aaa_price_hide_item }}{% else %}false{% endif %},
      membershipDiscountPercent: {% if membership_discount_percent %}{{ membership_discount_percent }}{% else %}0{% endif %},
      hasMembershipPricing: {% if has_membership_pricing %}{{ has_membership_pricing }}{% else %}false{% endif %},
      discountWillApply: {% if membership_discount_percent > 0 %}true{% else %}false{% endif %},
      matchedCollection: '{% if matched_collection %}{{ matched_collection }}{% else %}None{% endif %}'
    });

    {% if membership_discount_percent > 0 %}
      console.log('🎯 ✅ DISCOUNT SHOULD BE APPLIED!');
    {% else %}
      console.log('❌ NO DISCOUNT - Check customer login, tags, or shop configuration');
    {% endif %}

    console.log('🚀 === AAA MEMBERSHIP PRICING DEBUG END ===');
  }
</script>
